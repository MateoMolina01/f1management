<UserControl x:Class="f1management.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource ColorFondo}">

    <Grid>
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <Border Background="White" Padding="20" CornerRadius="10">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.3" />
                </Border.Effect>

                <StackPanel>
                    <!-- Título -->
                    <TextBlock Text="Gestión F1" FontSize="24" FontWeight="Bold" 
                       Foreground="{StaticResource ColorPrincipal}" 
                       HorizontalAlignment="Center" Margin="0,0,0,20"/>

                    <!-- Usuario -->
                    <TextBlock Text="Usuario" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtUsuario" Height="30" Margin="0,0,0,15"/>

                    <!-- Contraseña -->
                    <TextBlock Text="Contraseña" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <PasswordBox x:Name="txtPassword" Height="30" Margin="0,0,0,25"/>

                    <!-- Botón -->
                    <Button Content="Iniciar Sesión"
                    Background="{StaticResource ColorPrincipal}"
                    Foreground="White"
                    FontWeight="Bold"
                    Height="35"
                    Click="IniciarSesion_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>